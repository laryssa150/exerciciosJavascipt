//1. Dobrar os números

// Dado o array:
const numeros = [1, 2, 3, 4, 5];

// Use .map para criar um novo array com o dobro de cada número.

const dobrar = numeros.map((numero) => numero * 2);
console.log(dobrar);
