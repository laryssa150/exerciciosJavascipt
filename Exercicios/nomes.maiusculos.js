const nomes = ["ana", "joão", "carlos"];

// Use .map para transformar todos os nomes em letras maiúsculas.

const transformarNomes = nomes.map((nome) => nome.toUpperCase());
  console.log(transformarNomes);